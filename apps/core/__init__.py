from gpg import GPG
#from content import ContentCache, ContentStorage
from core.runtime import gpg, content_storage

"""
Initialize a project wide instance of GPG, ContentCache and ContentStorage
"""

#if not gpg:
#    gpg = GPG()
    
#if not content_storage:
#    content_cache = ContentCache('/tmp')
#    content_storage = ContentStorage('/tmp', content_cache)
